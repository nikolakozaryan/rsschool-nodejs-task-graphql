export const ProfilesMutations = {
  createProfile: {
    // (dto: CreateProfileInput!): Profile!
  },
  changeProfile: {
    // (id: UUID!, dto: ChangeProfileInput!): Profile!
  },
  deleteProfile: {
    // (id: UUID!): String!
  },
};
